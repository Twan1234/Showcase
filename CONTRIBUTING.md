# Contributing

Git + GitHub; access control and identity via GitHub. Link check-ins to an issue/ticket where possible (e.g. commit message: `Fix X (#123)`). Keeps changes traceable to a ticket and author.
